name: Questions
if: false
on: [pull_request_target]

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - id: setup-node
        name: Setup Node
        uses: actions/setup-node@v1
        with:
          node-version: 14.x

